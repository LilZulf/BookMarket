import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Spacer = ({widht, height}) => {
  return (
    <View style={{width : widht, height: height}}/>
  )
}

export default Spacer

const styles = StyleSheet.create({})